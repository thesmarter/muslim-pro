# include: package:flutter_lints/flutter.yaml
include: package:lint/strict.yaml # For production apps
# include: package:lint/casual.yaml # For code samples, hackathons and other non-production code
# include: package:lint/package.yaml # Use this for packages with public API

formatter:
  page_width: 100
  trailing_commas: preserve
# You might want to exclude auto-generated files from dart analysis
analyzer:
  exclude:
    - "**.freezed.dart"
    - "**.g.dart"
    - "lib/generated/**"
    - "packages/**"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

# You can customize the lint rules set to your own liking. A list of all rules
# can be found at https://dart-lang.github.io/linter/lints/options/options.html
linter:
  rules:
    # Util classes are awesome!
    avoid_classes_with_only_static_members: false

    # Centrally disabled: previously suppressed via ~50 mass file-level
    # `ignore_for_file` comments. Keeping them disabled here avoids noise
    # while preserving generated-file ignores (type=lint) elsewhere.
    public_member_api_docs: false
    sort_constructors_first: false
